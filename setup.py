from setuptools import setup, find_packages

setup(
    name='EmotionDetection',
    version='1.0.0',
    description='A library for emotion detection using Watson NLP',
    author='Your Name',
    packages=find_packages(),
    install_requires=[
        'requests'
    ],
)